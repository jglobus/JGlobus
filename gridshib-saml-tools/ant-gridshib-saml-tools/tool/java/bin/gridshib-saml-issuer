#!/bin/sh

## the GridShib installation directory
if [ ! -n "$GRIDSHIB_HOME" ] ; then
  echo "Error: GRIDSHIB_HOME is not defined."
  exit 1
fi

. $GRIDSHIB_HOME/bin/java org.globus.gridshib.tool.saml.SAMLAssertionIssuerTool "$@"

