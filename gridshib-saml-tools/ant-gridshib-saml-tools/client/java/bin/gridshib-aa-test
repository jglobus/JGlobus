#!/bin/sh

## This script must have UNIX-style end-of-line characters

## the IdP installation directory
if [ ! -n "$GRIDSHIB_HOME" ] ; then
  echo "Error: GRIDSHIB_HOME is not defined."
  exit
fi

$GRIDSHIB_HOME/bin/java org.globus.gridshib.client.saml.query.GridShibClient "$@"

exit
